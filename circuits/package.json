{
  "name": "tawf-did-circuits",
  "version": "1.0.0",
  "scripts": {
    "compile": "circom mustahik_eligibility.circom --r1cs --wasm --sym -o build/",
    "setup": "snarkjs groth16 setup build/mustahik_eligibility.r1cs pot12_final.ptau build/mustahik_eligibility_0.zkey",
    "contribute": "snarkjs zkey contribute build/mustahik_eligibility_0.zkey build/mustahik_eligibility_final.zkey --name='tawf-did phase2' -v",
    "export:vkey": "snarkjs zkey export verificationkey build/mustahik_eligibility_final.zkey build/verification_key.json",
    "export:solidity": "snarkjs zkey export solidityverifier build/mustahik_eligibility_final.zkey ../contracts/contracts/Groth16Verifier.sol",
    "copy:frontend": "cp build/mustahik_eligibility_js/mustahik_eligibility.wasm ../frontend/public/circuits/ && cp build/mustahik_eligibility_final.zkey ../frontend/public/circuits/ && cp build/verification_key.json ../frontend/public/circuits/",
    "test:proof": "node scripts/test_proof.js"
  },
  "devDependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  }
}
